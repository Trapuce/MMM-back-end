spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MMM
    username: traore
    password: traore223
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG
